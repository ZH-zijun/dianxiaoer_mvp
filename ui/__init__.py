"""
ui 包 — 店小二 UI 界面模块

子模块：
- login_screen.py   登录页（密码验证 + 首次改密）
- chat_screen.py    主对话框（对话气泡 + 唱诺高亮 + 录音按钮）
- settings_screen.py 设置页（API配置 / 偏好 / 备份）

设计约束（来源：用户 UI 规范要求）：
- 对话内容字号 >= 18sp
- 唱诺文案字号 >= 22sp，橙色醒目
- 按钮文字字号 >= 16sp
- 深色背景，高对比度，不依赖系统主题
- 输入区和录音按钮固定底部，对话区可滚动
- 录音按钮 >= 48dp x 48dp
"""

# ══════════════════════════════════════════════
# 配色方案
# ══════════════════════════════════════════════

# 背景
BG_DARK = (0.102, 0.102, 0.180, 1)       # #1a1a2e 深色主背景
BG_CARD = (0.176, 0.176, 0.267, 1)        # #2d2d44 卡片/输入区背景
BG_INPUT = (0.204, 0.204, 0.298, 1)       # #34344c 输入框背景

# 对话气泡
BUBBLE_USER = (0.96, 0.96, 0.96, 1)       # #f5f5f5 用户气泡（浅灰白）
BUBBLE_USER_TEXT = (0.13, 0.13, 0.13, 1)  # #212121 用户气泡文字（深色）
BUBBLE_AI = (0.20, 0.20, 0.32, 1)         # #333352 AI 气泡（深色）
BUBBLE_AI_TEXT = (1.0, 1.0, 1.0, 1)       # #ffffff AI 气泡文字（白色）

# 唱诺文案
CHANGNUO_COLOR = (1.0, 0.6, 0.0, 1)       # #ff9900 橙色醒目
CHANGNUO_BG = (0.35, 0.18, 0.0, 1)        # #592e00 深橙底色

# 状态栏
STATUS_BAR_RED = (0.9, 0.15, 0.15, 1)     # #e62626 网络中断红色

# 按钮
BTN_PRIMARY = (0.18, 0.55, 0.34, 1)       # #2e8c57 主按钮（深绿）
BTN_PRIMARY_TEXT = (1.0, 1.0, 1.0, 1)     # #ffffff 主按钮文字
BTN_DANGER = (0.8, 0.2, 0.2, 1)           # #cc3333 危险按钮
BTN_DANGER_TEXT = (1.0, 1.0, 1.0, 1)      # #ffffff
BTN_RECORD = (0.85, 0.25, 0.25, 1)        # #d94040 录音按钮（红）
BTN_RECORD_ACTIVE = (1.0, 0.3, 0.3, 1)    # #ff4d4d 录音中
BTN_TEXT = (1.0, 1.0, 1.0, 1)             # #ffffff 按钮文字

# 文字
TEXT_PRIMARY = (1.0, 1.0, 1.0, 1)         # #ffffff 主文字（白色）
TEXT_SECONDARY = (0.7, 0.7, 0.8, 1)       # #b3b3cc 次要文字
TEXT_HINT = (0.5, 0.5, 0.6, 1)            # #808099 提示文字
TEXT_ERROR = (1.0, 0.4, 0.4, 1)           # #ff6666 错误提示
TEXT_SUCCESS = (0.4, 0.9, 0.5, 1)         # #66e680 成功提示

# 分隔线
DIVIDER = (0.3, 0.3, 0.42, 1)             # #4d4d6b

# ══════════════════════════════════════════════
# 字号规范（sp）
# ══════════════════════════════════════════════

FONT_TITLE = 24          # 页面标题
FONT_CHANGNUO = 22       # 唱诺文案（醒目，>= 22sp）
FONT_CHAT = 18           # 对话内容（>= 18sp）
FONT_CHAT_SMALL = 16     # 对话时间戳/次要信息
FONT_BTN = 16            # 按钮文字（>= 16sp）
FONT_BTN_LARGE = 18      # 大按钮文字
FONT_INPUT = 18          # 输入框文字
FONT_HINT = 14           # 提示/辅助文字
FONT_STATUS = 14         # 状态栏文字
FONT_LABEL = 16          # 标签文字

# ══════════════════════════════════════════════
# 间距规范（dp）
# ══════════════════════════════════════════════

PADDING_X = 16           # 水平内边距
PADDING_Y = 12           # 垂直内边距
PADDING_SMALL = 8        # 小间距
PADDING_LARGE = 20       # 大间距
MARGIN_BUBBLE = 8        # 气泡间距
MARGIN_BOTTOM = 10       # 底部输入区上方间距

# ══════════════════════════════════════════════
# 组件尺寸（dp）
# ══════════════════════════════════════════════

BTN_HEIGHT = 48          # 按钮最小高度（>= 48dp）
BTN_RECORD_SIZE = 56     # 录音按钮尺寸（>= 48dp）
INPUT_HEIGHT = 44        # 输入框高度
STATUS_BAR_HEIGHT = 36   # 状态栏高度
BUBBLE_RADIUS = 12       # 气泡圆角
BUBBLE_MAX_WIDTH = 0.85  # 气泡最大宽度（相对屏幕）

# ══════════════════════════════════════════════
# 动画
# ══════════════════════════════════════════════

ANIM_DURATION = 0.3      # 默认动画时长（秒）
